//const serverURL = "localhost:2140"
//const serverURL = "paint_multi.153070065.xyz"
const serverURL = "megapaint.153070065.xyz"
const fullURL = `${(serverURL.startsWith("127.0.0.1") || serverURL.startsWith("localhost") ? "http" : "https")}://${serverURL}`